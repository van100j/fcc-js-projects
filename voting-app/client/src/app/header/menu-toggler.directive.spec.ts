import { MenuTogglerDirective } from './menu-toggler.directive';

describe('MenuTogglerDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuTogglerDirective();
    expect(directive).toBeTruthy();
  });
});
